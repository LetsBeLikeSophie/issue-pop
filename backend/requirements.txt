feedparser>=6.0
scikit-learn>=1.3
numpy>=1.24
kiwipiepy>=0.18
sentence-transformers>=3.0
fastapi>=0.110
uvicorn>=0.29
sqlmodel>=0.0.22
requests>=2.31  # 날씨 카드용 Open-Meteo 조회(api.py의 _fetch_weather)
bcrypt>=4.1  # 로그인 비밀번호 해시(api.py의 _hash_password/_verify_password)
slowapi>=0.1.9  # 로그인/가입 무차별 대입 방지 rate limiting
firebase-admin>=6.5  # 다이제스트 등 실제 푸시 발송(FCM). 서버에
# GOOGLE_APPLICATION_CREDENTIALS 환경변수로 서비스 계정 키 JSON 경로를
# 줘야 함 — 키 파일 자체는 git에 안 올림(backend/README.md 참고).
# Windows에서 설치가 "파일 이름이나 확장명이 너무 깁니다"로 실패하면
# (torch 라이선스 폴더 경로가 260자 제한을 넘는 경우) 훨씬 짧은 경로에
# 따로 설치하세요: pip install --target=pylibs sentence-transformers
# embedding_utils.py가 backend/pylibs를 자동으로 sys.path에 추가함.
