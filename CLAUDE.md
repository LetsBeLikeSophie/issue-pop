# 프로젝트 컨텍스트 (Claude Code용 인계 문서)

이 파일은 Cowork(모바일)에서 진행하던 대화를 Claude Code로 옮기면서 만든
인계 문서예요. Claude Code 세션은 그 대화 내용을 기억 못 하니까, 지금까지
정한 것과 진행 상황을 여기 정리해뒀어요. Claude Code를 이 폴더에서 실행하면
이 파일을 자동으로 읽으니, 새로 설명 안 해도 이어서 작업할 수 있어요.

## 프로젝트가 뭔지

뉴스 트렌드 앱. 여러 매체의 RSS를 모아서 "오늘 어떤 이슈가 많이 보도됐는지"
클러스터링해서 보여주는 앱이에요. 두 부분으로 나뉘어요:

- **모바일 클라이언트 (플러터)** — 아직 코드로 안 옮김. 디자인은
  `design/` 폴더의 와이어프레임으로 이미 확정돼 있음.
- **백엔드 (파이썬)** — RSS 크롤링 + 이슈 클러스터링 + 트렌드 카운트.
  `backend/` 폴더. 지금 이 작업의 핵심이고, 진행 중.

## 왜 이 순서로 가는지

"이 앱에서 제일 먼저 검증해야 할 기술"로 이슈 클러스터링을 꼽았어요.
그게 실제로 되는지 확인하기 전엔 화면(플러터)을 붙이는 의미가 없다고
판단해서, 백엔드 핵심 로직(파이썬)부터 만들고 검증한 다음에 플러터로
넘어가기로 했어요. 지금 아직 플러터는 손 안 댔고, 그게 정상이에요.

기술 선택 이유:
- **플러터**: 혼자 개발하면서 iOS/Android 둘 다 커버해야 하는데, 이 앱은
  구글/애플 기본 UI가 아니라 완전히 커스텀 디자인(둥근 스티커 카드,
  회전된 뱃지 등)이라서 위젯 단위로 스타일을 세밀하게 통제하기 쉬운
  플러터가 리액트 네이티브보다 손이 덜 감. 핫 리로드도 디자인 미세조정
  작업 스타일과 잘 맞음.
- **파이썬 (백엔드)**: 텍스트 유사도 비교, RSS 파싱, 스케줄링 라이브러리가
  풍부하고, 나중에 임베딩 기반 클러스터링으로 정교화할 때 생태계가 유리함.

## 디자인 (design/ 폴더)

`.dc.html` 파일들은 와이어프레임 디자인 도구로 만든 화면 목업이에요
(390×844, 모바일 프레임 기준). 코드 안에 인라인 스타일 + CSS 변수로
디자인 시스템이 정의돼 있어요.

- `Main.dc.html` — 홈 (지속 관심 이슈 + 오늘 많이 언급된 키워드 랭킹)
- `IssueDetail.dc.html` — 이슈 상세 (매체별 보도 분포 + 전체 관련기사)
- `Archive.dc.html` — 아카이브 (팔로우한 이슈 목록)
- `Search.dc.html` — 검색 (실시간 인기 검색어 + 최근 검색)
- `Settings.dc.html` — 설정 · 구독
- `DirectionA/B/C.dc.html` — 참고용 (컨셉 탐색 과정 기록, 최종적으로 C
  "스티커 팝" 컨셉을 선택해서 위 5개 화면에 적용함. 더 손댈 필요 없음)
- `canvas.json` — 화면 배치 + 기획 메모 (각 화면의 의도, 아직 미정인 것들)

**디자인 컨셉 (선택됨)**: PANTONE 11-4201 Cloud Dancer 배경 +
세이지그린(`#7c8f6e`)/테라코타(`#b5703f`) 액센트, 둥근 스티커 카드,
회전된 배지. 색은 지금 5개 파일에 각각 CSS 변수로 따로 선언돼 있어서
(`--color-bg`, `--color-accent-1/2` 등) 값 하나 바꾸려면 5곳을 다 고쳐야
함 — 플러터로 코드화할 때 공용 테마 파일 하나로 합칠 것.

**아직 미정인 것**:
- 정치 카테고리 성향 분류 — 자체 판정 대신 기존 미디어 연구기관의 분류를
  출처 표기하며 인용하는 방식을 권장 (canvas.json 메모 참고)
- 구독 가격 (Settings 화면에 placeholder로 남겨둠)
- `.dc.html`의 `<script src="./support.js">`는 와이어프레임 뷰어 도구용
  헬퍼라 실제로는 존재하지 않는 파일임 — 디자인 참고용으로만 열어보면
  스타일은 그대로 보임 (브라우저에서 스크립트 에러는 무시해도 됨).

## 백엔드 (backend/ 폴더) — 지금까지 한 것

`backend/README.md`에 훨씬 자세히 정리돼 있음 (검증 과정, 발견한 문제,
한계까지). 요약만 하면:

- `fetcher.py` — RSS 수집.
- `sources.py` — 매체 목록. 2026-08-23에 로컬 PC(실제 인터넷)에서
  `validate_sources.py`를 처음 돌려서 20개 중 9개 생존 확인, 죽은 11개는
  제거하고 살아있는 9개는 `confidence: "verified"`로 표시함.
- `clustering.py` — 핵심 로직. 문자 n-gram TF-IDF로 시작했다가 2026-08-24에
  **한국어 문장 임베딩(`jhgan/ko-sroberta-multitask`) + 코사인 유사도 +
  AgglomerativeClustering(complete-linkage)**로 교체함. **threshold=0.45.**
  계기: 매체마다 표현이 완전히 다른 같은 이슈를 문자 n-gram이 놓치는
  경우가 있었음(예: "이소희·백하나...31년 만에 우승" vs "백하나-이소희...
  31년 만의 金" — 겹치는 글자가 거의 없음). 범용 다국어 임베딩 모델도
  시도했는데 매체별 "문체"를 너무 비슷하다고 판단해서 폐기, 한국어 STS
  전용 모델로 감. 이 과정에서 한겨레 RSS `summary`에 실제 요약 대신
  썸네일 이미지용 HTML 태그가 그대로 들어있는 데이터 버그도 발견해서
  `text_utils.py`에서 고침 — "한겨레 기사들이 문체가 비슷해서 뭉친다"고
  오해했던 문제의 진짜 원인이었음. 자세한 모델 비교/튜닝은
  `backend/README.md`의 "클러스터링 엔진을 문자 n-gram → 한국어 문장
  임베딩으로 교체" 항목 참고. (Union-Find(single-linkage)의 구조적
  결함으로 459건 중 80%가 거대 클러스터로 뭉쳤던 문제 때문에 애초에
  complete-linkage를 쓰기로 한 결정은 임베딩으로 넘어온 지금도 유효함.)
- `embedding_utils.py` — 2026-08-24 추가. clustering.py/meta_cluster.py가
  공유하는 문장 임베딩 유틸. Windows에서 `sentence-transformers`(torch
  포함) 설치가 경로 길이 제한(260자)으로 실패해서 `backend/pylibs`에
  별도 설치하고 그 경로를 자동으로 `sys.path`에 추가하는 우회 로직이 있음.
- 랭킹 정렬 기준도 `article_count`에서 **`outlet_count` 우선**으로 바꿈
  (한 매체가 도배하는 이벤트가 "많이 보도된 이슈"로 잘못 보이는 문제 방지).
- `keyword_extraction.py` — 클러스터의 짧은 대표 키워드를 뽑는 로직.
  이것도 2026-08-23에 kiwipiepy 명사 추출로 교체함. "이슈판" 아티팩트로
  라이브 결과를 훑어보다가 "총리"/"포토"/"전체" 같은 의미 없는 키워드를
  발견해서 고침 — clustering.py의 바이라인/태그 제거 전처리를 재사용
  안 하던 것("포토"가 살아남은 이유), tf\*idf 점수인데 실제로는 raw
  빈도가 1순위였던 것("총리"가 이긴 이유) 두 가지가 원인이었음. 공용
  전처리는 순환참조 방지를 위해 `text_utils.py`로 뺐음. 자세한 내용은
  `backend/README.md`의 "업데이트: 키워드 추출도 kiwipiepy로 교체" 참고.
  **2026-08-24 추가 개선**: 키워드 하나만으론 무슨 뉴스인지 안 보인다는
  피드백으로 `extract_keywords()`를 새로 만들어 1등과 안 겹치는 2등
  키워드까지 뽑음(`cluster_articles()`의 `keywords` 리스트 필드).
  "38도"/"3%" 같은 숫자+단위 조합도 후보에 추가함(`text_utils.py`의
  `extract_noun_ngrams()` 확장). 겸사겸사 "마운자로"(비만치료제)가
  사전에 없어서 "마운자"+"로"(조사)로 잘못 쪼개지던 형태소 분석 버그도
  발견해서 `kiwi.add_user_word()`로 고침(`text_utils.py`의
  `_USER_WORDS` 리스트, 발견하는 대로 계속 추가하면 됨).
- `category.py` — 2026-08-23에 새로 추가. 이슈가 90개쯤 나오니 flat한
  목록이 한눈에 안 들어온다는 피드백을 받고, 유사도 기반 재클러스터링
  대신 사람이 익숙한 고정 뉴스 섹션(정치/경제/사회/국제/스포츠/연예/
  IT과학/기타)으로 분류하는 규칙 기반 분류기를 만듦(카테고리별 키워드
  사전과 클러스터 명사 토큰의 겹침으로 판정). `cluster_articles()` 결과에
  `category` 필드가 추가됨. "이슈판" 아티팩트에 카테고리 색깔 배지 +
  필터 칩도 붙였음.
- `meta_cluster.py` — 2026-08-23 추가, "이슈가 너무 많으니 더 큰 묶음으로
  안 되냐"는 요청으로 만든 2단계 재클러스터링(이슈들을 문서 취급해서
  한 번 더 complete-linkage). **2026-08-24: "이슈판" 아티팩트에서는 더
  이상 안 씀** — 슈퍼그룹 아코디언 UI를 실제로 써보니 그룹 라벨이 그룹
  내용을 대표 못 하는 문제가 있었음(라벨을 그룹 내 outlet_count 1등
  이슈의 키워드로 그냥 가져다 쓰는 방식이라, 묶인 이슈들을 아우르는
  새 단어가 아니었음)는 피드백을 받았고, 애초에 계층 구조 자체를
  원하지 않는다는 게 더 명확한 요청이었음. 코드는 삭제 안 하고
  남겨뒀음(`pipeline.py --group`으로 여전히 실행 가능, 나중에 라벨링을
  제대로 만들면(LLM 요약 등) 재활용 가능). 자세한 경위는
  `backend/README.md`의 "이슈판 아티팩트를 다시 평평한 목록으로 되돌림"
  참고.
- `pipeline.py` — 전체 실행 (`python3 pipeline.py`로 샘플 데이터 검증,
  `--live`로 실제 RSS, `--group`으로 슈퍼그룹 단위 — 단, 아티팩트
  기본 화면에서는 평평한 목록만 씀).
- `sample_data/sample_articles.json` + `test_pipeline.py` — 정답을 아는
  17건 픽스처로 클러스터링/키워드 추출 결과를 검증하는 회귀 테스트.
  지금 다 통과 상태 (`python3 test_pipeline.py`).

**검증된 것**: 샘플 데이터(17건)로도, 로컬 PC에서 실제 RSS(460건, 9개
매체)로도 클러스터링이 말이 되게 동작함을 확인함. 임베딩 기반으로 바꾼
뒤로는 매체마다 표현이 완전히 다른 같은 이슈(배드민턴 우승 기사 등)도
정확히 잡아내고, 예전에 골치였던 percolation류 대형 아티팩트도 거의
사라짐(최대 이슈 클러스터가 12건 수준).

**아직 남은 것**:
- "이슈판" 아티팩트는 매체 커버리지 상위 30~50개(기본 40개)만 표시함 —
  전체 이슈 목록(현재 실측 기준 250개 안팎)을 다 보여주진 않음.
- 슈퍼그룹(2단계) 재클러스터링 코드(`meta_cluster.py`)는 남아있지만
  라벨링이 부실해서 지금은 안 씀 — 나중에 LLM 요약 등으로 라벨을
  제대로 만들면 다시 쓸 만함.
- 임베딩 인코딩이 느림(460건에 20~80초) — 실서비스로 가면 캐싱/배치
  처리 필요.

- `api.py` — 2026-08-24 추가. `uvicorn api:app --reload`로 실행.
  요청마다 클러스터링을 다시 안 돌리고, 서버 시작 시 한 번 + 30분마다
  백그라운드에서 재수집·재클러스터링해서 캐시를 채우고 API는 그 캐시만
  읽음(그래서 요청 자체는 가벼움 — 인프라 스펙 요구사항이 낮아지는
  이유이기도 함, 라즈베리파이/저가 VPS 논의는 이 세션 메모리 참고).
  `GET /trending`(랭킹, limit/category 파라미터), `GET /issues/{id}`
  (상세, id는 기사 링크 해시), `GET /categories`, `GET /health`,
  `POST /refresh`(수동 갱신, 개발용). 로컬에서 4개 엔드포인트 다 실제
  호출해서 검증함. 자세한 내용은 `backend/README.md`의 "업데이트:
  FastAPI 엔드포인트 추가" 참고.
- `db.py` — 2026-08-24 추가. SQLModel+SQLite(`news_trend.db`, 런타임에
  생성되는 파일이라 저장소에 커밋 안 함). 나중에 기능을 더 붙일 때
  테이블을 안 갈아엎으려고 앞으로 필요해질 기능들을 염두에 두고 미리
  짠 스키마 — `issues`/`articles`(클러스터링 결과 영속화),
  `cluster_summaries`(요약 텍스트 캐시, 이슈당 1번만 생성하는 캐싱
  구조), `devices`/`device_keyword_watches`(로그인 없이 키워드 알림
  보내는 용도), `users`/`subscriptions`(로그인/결제, 지금은 비어있는
  게 정상), `user_favorites`(즐겨찾기, 로그인 전까진 비어있음 — 로그인
  안 한 유저의 즐겨찾기는 클라이언트 로컬 저장이 1차 저장소).
  `api.py`에 새 엔드포인트(`/archive`, `/devices`, `/watches`,
  `/favorites`, `/issues/{id}/summary`) 추가해서 전부 실제 호출로
  검증함. 자세한 내용/알려진 한계(즐겨찾기 id 불안정성 등)는
  `backend/README.md`의 "업데이트: DB 스키마 설계 + 영속화" 참고.
  제품 기획(가격/유료-무료 경계 등) 관련 내용은 이 저장소 밖에 별도로
  정리해둠(`MONETIZATION.md`, git 추적 안 함).

## 다음 할 일 (우선순위 순)

1. 인프라 확정하고 배포(Oracle Free Tier 등 — 이 세션 메모리 참고,
   FastAPI 코드 자체는 인프라 선택과 무관하게 그대로 씀).
2. `category.py`의 "기타" 분류 비율(현재 약 24%) 개선.
3. 그 다음에 플러터로 `design/` 폴더의 화면들을 옮기기 시작.

## 톤/작업 스타일 참고

지금까지 이 프로젝트는 "일단 만들어서 돌려보고, 실측해서 문제를 찾고,
고친 이유를 남기는" 식으로 진행해왔어요 (threshold 0.35→0.12 튜닝이
대표적 예). 다음 세션도 이 방식을 유지하면 좋아요 — 감으로 값을
정하지 말고 실제로 돌려서 확인.
